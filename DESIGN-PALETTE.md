# SoSolsunday 配色方案 — 脫離 OkinawaSundays

> 2026-09-03。DESIGN.md 是版型與結構規格，這份只談**顏色**。
> 已實作方案 A，檔案在 `docs/assets/palette-terracotta.css`，五個頁面都已 link，打開就能看。

---

## 0. 為什麼要換

v2 直接沿用 OkinawaSundays 的色票（`#062a3a` 深海藍 + `#79d9e6` 亮青 + `#ffd447` 黃 + `#fff7e8` 奶油底）。
好處是一眼看得出是同一個家族，壞處是**兩個站放在一起會分不出來** —— OkinawaSundays 講一個島，SoSolsunday 講整個亞洲的機酒與假期，個性應該不一樣。

版型、字級、圓角、陰影、元件全部不動，**只換顏色**，因為版型那套已經驗收過了。

---

## 1. 四張參考圖的判讀

| 參考 | 核心配色 | 判斷 |
|---|---|---|
| Fly off App（薄荷 × 珊瑚 × 粉膚底） | `#3DD7C6` / `#EC8C6F` / `#F7E0DC` | 好看，但薄荷跟 OkinawaSundays 的亮青太近，**換了等於沒換**；粉膚底也偏美妝／wellness |
| 威尼斯運河 | 奶油白 / 杏橘 / 陶土 / 珊瑚紅 / 墨綠 / 湖藍 | ✅ 方向對 |
| New Orleans 街景 | `#FFF6ED` / 鴨綠 / 陶土橘 / 磚紅 / 芥末金 | ✅ 方向對，而且**跟威尼斯是同一組家族** |
| FANA World Travel | 酒紅 / 米駝 / 淺灰藍 / 祖母綠 / 深藍 | 高級旅行社的氣質，偏 editorial；酒紅會跟「價格偏貴」的紅撞在一起 |

**你自己挑的四張裡有兩張是同一個方向**（威尼斯 + New Orleans）——都是「**陶土牆 × 鴨綠百葉窗 × 象牙底 × 芥末點綴**」。這不是巧合，是偏好。所以我照這個方向做。

---

## 2. 推薦：方案 A「地中海街景」Terracotta × Sage

**已實作，可以直接看。**

| 變數 | 新值 | 舊值（沖繩） | 角色 |
|---|---|---|---|
| `--sand` | `#FBF4EC` | `#fff7e8` | 頁面底：暖象牙，少黃、多一點粉土感 |
| `--paper` | `#FFFCF7` | `#fffefb` | 卡片底 |
| `--navy` | `#1E4A45` | `#062a3a` | **深墨綠**（原深海藍）：footer／主按鈕／年曆招牌 |
| `--navy-2` | `#2B615A` | `#0b4052` | 漸層第二段 |
| `--ink` | `#26312E` | `#112f3b` | 內文黑，帶綠棕 |
| `--muted` | `#6E6459` | `#5d737a` | 次要文字：暖灰褐 |
| `--line` | `rgba(38,49,46,.14)` | `rgba(6,42,58,.13)` | 分隔線 |
| **`--blue`** | **`#B84E29`** | `#087f9c` | ★ **變數名沿用，實際是深陶土**：連結／eyebrow／hover 邊框／focus ring。**整站個性從「海」變「街」，就靠這一個** |
| `--lagoon` | `#8FBFB2` | `#79d9e6` | 裝飾色塊：鼠尾草綠 |
| `--lagoon-soft` | `#E6F1EC` | `#dff7f8` | 標籤淺底 |
| `--coral` | `#CF5730` | `#ff665a` | 陶土橘：週末日期、假期人潮提醒、brand-mark 漸層 |
| `--sun` | `#E8A62F` | `#ffd447` | 芥末金：螢光筆底線、年曆 CTA、深綠底上的亮字 |

### 語意色（價格）—— 這裡有個坑，要特別處理

品牌變暖色之後，「橘＝品牌」和「橘＝貴」會打架。所以價格三階**刻意不用品牌色**：

| 變數 | 新值 | 意思 |
|---|---|---|
| `--good` | `#1F7A63` | 最低價 —— 全站只有它是綠的，最跳 |
| `--warn` | `#7A6C5C` | 中間價 —— 中性灰褐，刻意不搶眼 |
| `--bad` | `#8E2F22` | 最貴 —— 深氧化紅，比品牌陶土更深更暗，不會被當成連結 |

比舊版的 綠／琥珀／紅 更清楚：**只有便宜的會發光，貴的沉下去，中間的閉嘴**。也對色盲友善。

### 陰影
```css
--shadow:0 18px 50px rgba(74,52,36,.13);
--shadow-sm:0 9px 24px rgba(74,52,36,.10);
```
陰影從冷灰改成暖褐，卡片浮在象牙底上才不會發灰。

### 顏色以外的三處連動
1. **Hero 裝飾對調**：右上大色塊從鼠尾草改**陶土**（`opacity:.20`），左下圓環從珊瑚改**鼠尾草**。這是最快讓頁面「變暖」的一招。
2. **底部海浪退到背景**：`opacity:.42`、高度降到 92px、重新染成鼠尾草／墨綠。它現在讀起來是地平線，不是沖繩那道亮青浪。
   *（如果想更徹底切開，可以把海浪整個拿掉，改成 hero 右上一圈暖陽光暈 —— 這個我沒做，要的話再說。）*
3. **年曆招牌**：漸層改 `#1E4A45 → #2B615A → #3A7F6E`，光暈從青色改芥末金。

### 對比度
15 組主要文字／底色組合全部通過 WCAG AA（實測值）：

```
內文 12.33  次要 5.30  陶土連結 4.63  最低價 5.10  中間價 4.97
最貴 7.95   芥末字在深綠 4.68  頁尾內文 5.72  主按鈕白字 9.90
```

---

## 3. 備選方案（要換方向再說，我可以照做）

### B「薄荷氣泡」Mint × Coral on Blush（Fly off App）
```
--sand:#F9EAE6  --paper:#FFFFFF  --navy:#33403F
--blue:#2FB3A3  --lagoon:#7FE0D3  --lagoon-soft:#E2F7F4
--coral:#E4785A  --sun:#F2C14E
```
輕、年輕、app 感。**缺點**：薄荷離 OkinawaSundays 的亮青太近，換完還是像同一站；粉膚底在價格表上會偏甜。

### C「夜航奢遊」Wine × Emerald × Navy（FANA）
```
--sand:#F2EBE1  --paper:#FFFDFA  --navy:#1B2A4A
--blue:#0E7C6B  --lagoon:#BFCBD6  --lagoon-soft:#E8EEF3
--coral:#8E1F3C  --sun:#C9A227
```
高級旅行社的質感，適合走「精選行程」而不是「比價速報」。**缺點**：酒紅跟「價格最貴」的紅撞，價格表要整組重想；整體偏重，跟站名的 ☀ 不合。

---

## 4. 施工狀態：✅ 已折回主檔（2026-09-04）

配色已定案並折進 `docs/assets/sosol.css`，`palette-terracotta.css` 覆蓋層與六個頁面的 `<link>` 都已移除。
現在**唯一的色票來源就是 `sosol.css` 開頭的 `:root`**，換色只改那裡的值。

年曆頁（`docs/calendar/index.html`）自帶一份 `:root`，因為它跟 `SunFamilyTrip/calendar.html` 是同一份檔案，
所以本站的配色走 `docs/calendar/calendar-theme.css` 覆蓋；改主色時兩邊要一起改。

### 三條紅線
- **不要改 `docs/calendar/index.html`。** 它跟 `SunFamilyTrip/calendar.html` 是同一份檔案（SHA-1 必須一致），年曆頁的配色只能走 `calendar-theme.css`。
- **不要動 `--good` / `--warn` / `--bad` 去遷就品牌色。** 價格語意色故意跟品牌色拉開，這是設計決定不是疏忽。
- **不要新增變數名。** 全部沿用既有的 12 個 token，只換值，這樣 `sosol.css`、`site.js`、四個頁面、`calendar-theme.css` 都不用改結構。

### 驗收
```sh
node --test tests/frontend.test.cjs   # 14 項應全過（顏色不影響邏輯）
```
外加目視：
- [ ] 頁面底色 `#FBF4EC`，不是 `#fff7e8`
- [ ] eyebrow（FLIGHTS / DESTINATIONS…）與 H1 第二行是陶土色，底線是芥末金
- [ ] 價格表：最低價綠色帶 ★、中間灰褐、最貴深紅，三者一眼分得出來
- [ ] 年曆頁的月曆格：共同假日深綠、週末陶土、建議窗口虛線框
- [ ] 375px 無橫向捲動、console 無 error
